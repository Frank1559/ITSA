#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
#include <algorithm>
#include <cmath>
using namespace std;

int main(){
    int x1, x2=0, y1=0, y2=0, i, a;
    string str, t;
    double y=0;
    vector<int> v(10);
    
    while(cin >> y1){
        if(y1 > 31) cout << "Value of more than 31" << endl;
        else{
            x1 = 2 << (y1-1);
            cout << x1 << endl;
        }
    }
    return 0;
}
