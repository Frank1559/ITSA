#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
#include <algorithm>
#include <cmath>
using namespace std;

int main(){
    int x1, x2=0, y2=0, i, a;
    string str, t;
    double y=0, y1;
    vector<int> v(10);
    
    while(cin >> y1){
        y = y1 * 9.0 / 5.0 + 32.0;
        cout << fixed  <<  setprecision(1) << y << endl;
    }
    return 0;
}