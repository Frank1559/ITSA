#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
#include <algorithm>
#include <cmath>
using namespace std;

int main(){
    int x1, x2=0, y1=0, y2=0, i, a;
    string str, t;
    double y=0;
    vector<int> v;
    
    while(cin >> y1){
        x1 = 0;
        for(i=1;i<=y1;i++){
            if((i % 2 == 0) && (i % 3 == 0) && (i % 12 != 0)){
                x1 += i;
            }
        }
        cout << x1 << endl;
    }
    return 0;
}