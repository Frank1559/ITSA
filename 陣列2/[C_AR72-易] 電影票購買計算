#include    <stdio.h>
#include    <string.h>

int main(){   
    int cnt=0, i, j, n, a, b;
    
    scanf("%d", &n);
    while(scanf("%d %d", &a, &b) != EOF){
        printf("%d\n", a * 250 + b * 175);
    }
    return 0;
}
