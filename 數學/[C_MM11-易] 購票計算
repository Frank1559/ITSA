#include <stdio.h>

int main(){
    int bottom;
    double result, a;
    
    while(scanf("%d", &bottom) != EOF){
        
        printf("NT10=%d\nNT5=%d\nNT1=%d\n", bottom / 10, bottom % 10 / 5, bottom % 10 % 5);
    }
    return 0;
}
