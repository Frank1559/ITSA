#include <stdio.h>

int main(){
    int bottom;
    int result, a;
    
    while(scanf("%d", &bottom) != EOF){
        result = ((double) bottom * 100) / (100 - (30 * 2.54));
        printf("%d\n", result + 1);
    }
    return 0;
}
