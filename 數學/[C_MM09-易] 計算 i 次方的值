#include <stdio.h>

int main(){
    int b, a;
    int result;
    
    while(scanf("%d", &a) != EOF){
        result = 1 << a;
        if(a > 31) printf("Value of more than 31\n");
        else printf("%d\n", result);
    }
    return 0;
}
