#include <stdio.h>

int main(){
    double bottom;
    double result, a;
    
    while(scanf("%lf", &bottom) != EOF){
        result = bottom  * 9 / 5 +32;
        a=((int)((result*100+5)/10))/10.0;
        printf("%.1f\n", a);
    }
    return 0;
}
